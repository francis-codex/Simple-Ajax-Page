# Simple-Ajax-Page

A small site that swaps Home, About and Contact content without full page reloads, using AJAX (`script.js`) with a PHP endpoint (`server.php`).

Serve the folder with PHP:

```bash
php -S localhost:8000
```

Then open http://localhost:8000.
